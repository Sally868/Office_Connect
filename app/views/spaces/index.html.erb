<div style="height: calc(100vh - 70px)">

    <div class="p-10">
      <% @spaces.each do |space| %>
        <%= render 'space', space: space %>
      <% end %>
         <div class="flex justify-end">
      <%= link_to 'Add Space', new_space_path(@space), class:'py-2 px-4 no-underline bg-blue-200 text-blue-900 rounded-md' %>
    </div>
    </div>

</div>
