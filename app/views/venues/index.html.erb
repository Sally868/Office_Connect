<div style="height: calc(100vh - 70px)">

    <div class="p-10">
      <% @venues.each do |venue| %>
        <%= render 'venue', venue: venue %>
      <%= link_to 'Add Venue', venue_spaces_path(venue), class:'py-2 px-4 no-underline bg-blue-200 text-blue-900 rounded-md' %>
      <% end %>
         <div class="flex justify-end">
    </div>
    </div>

</div>
