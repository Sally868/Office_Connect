 <div class="w-full flex mb-4 p-2 rounded-2xl hover:bg-gray-100">
  <div class="p-2 pl-4 flex flex-col justify-between flex-grow-1">
    <div class="flex justify-between">
      <div>
        <h2 class="text-xl mt-1 text-white"><%= venue.name %></h2>
        <h3 class="text-xs px-2 py-1 bg-blue-200 text-blue-600 inline rounded-md"><%= venue.address %></h3>
      </div>
    </div>
    <div class="flex justify-end">
      <%= link_to 'Book the space', venue_path(venue), class:'py-2 px-4 no-underline bg-yellow-200 text-yellow-900 rounded-md' %>
    </div>
   
  </div>
 </div>
